OPENROUTER_API_KEY="your_openrouter_api_key_here"
LANGCHAIN_DEBUG="true"

# Model name/temperature/max_tokens/top_p/etc. per agent are configured in the
# ```yaml blocks of src/resources/prompts/*.md, which take precedence over
# any PLANNER_*/VISION_*/TEXT_* env vars. See ModelConfig in utils/config.py
# for the fallback defaults used when a prompt file doesn't set a field.
